\documentclass[14pt, a4paper]{extarticle}

% Підтримка української мови та кирилиці
\usepackage[T2A]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[ukrainian]{babel}
\usepackage{amsmath, amssymb}

% Налаштування полів (робимо трохи ширші для зручності тримання в руках)
\usepackage[left=2.5cm, right=2cm, top=2cm, bottom=2cm]{geometry}

% Півторний інтервал для зручного читання з аркуша
\usepackage{setspace}
\onehalfspacing

% Абзацний відступ
\usepackage{indentfirst}
\setlength{\parindent}{1.25cm}

% Вимкнення нумерації сторінок (за бажанням)
\pagestyle{plain}

\begin{document}
	
	\begin{center}
		\textbf{\Large Текст доповіді до дипломної роботи} \\
		\vspace{0.5cm}
		\large Тема: «Дослідження та програмна реалізація методу масштабування зображень з використанням барицентричної формули на основі вузлів інтерполяції Чебишева»
	\end{center}
	
	\vspace{1cm}
	
	\textbf{[СЛАЙД 1-2] Формулювання теми та актуальність}
	
	Задачі масштабування зображень є критично важливими у сферах геоінформаційних систем (ГІС), обробки супутникових знімків, медичної візуалізації та комп'ютерного зору. Від вибору алгоритму масштабування безпосередньо залежить баланс між обчислювальною складністю та якістю зображення.
	
	Актуальність нашого дослідження зумовлена тим, що з розвитком цифрових технологій зростають і вимоги до якості графіки. Нам постійно доводиться адаптувати зображення під різні формати екранів: від маленьких дисплеїв смартфонів до великих 4K-моніторів. При цьому в таких сферах, як медична візуалізація, супутникові знімки чи комп'ютерний зір, втрата навіть дрібних деталей під час зміни розміру картинки є критичною.
	
	Проблема полягає в тому, що більшість існуючих стандартних алгоритмів (таких як білінійна чи бікубічна інтерполяція) є компромісними. Вони працюють швидко, але часто призводять до розмиття зображення або появи артефактів на різких контурах об'єктів. З іншого боку, складніші глобальні математичні методи раніше вважалися або занадто повільними для комп'ютерної графіки, або страждали від крайових спотворень, відомих як феномен Рунге. Вони не вирішують в повній мірі сучасні задачі.
	
	Наша робота полягає у спробі створення нового методу, який дозволить обійти ці недоліки, уникнути артефактів та зберегти високу структурну точність пікселів (особливо при сильному зменшенні зображень), це є надзвичайно актуальною задачею, яку ми і вирішуємо у цій роботі. Інтерполяційні формули за вузлами Чебишева широко використовуються в математиці, і також можуть бути використані для задачі масштабування зображень.
	
	Метод, який ми представляємо, працює як у напрямку зменшення, так і збільшення зображень, у тому числі для великих коефіцієнтів масштабування. Це неадаптивний метод, що належить до класу методів інтерполяції. Більшість класичних методів зазвичай базуються на рівномірних сітках вузлів і розглядають лише локальну інтерполяцію, а не глобальну.
	\vspace{0.5cm}
	
	\section{Базові методи}
	Перш ніж перейти до нашого методу, варто коротко нагадати про три класичні алгоритми просторового масштабування, які є індустріальним стандартом.
	
	Перший і найпростіший метод — інтерполяція за найближчим сусідом. Її принцип полягає у банальному дублюванні кольору того пікселя, який геометрично знаходиться найближче до цільової точки. Цей метод надзвичайно швидкий, але він жахливо спотворює зображення, створюючи "сходинки" на похилих лініях — так званий аліасинг.
	
	Другий підхід — білінійна інтерполяція. Тут для розрахунку кольору береться вже околиця 2 на 2 пікселі, і нове значення обчислюється як їхня зважена сума. Результат виходить набагато плавнішим, але метод має критичний недолік: він діє як фільтр низьких частот, сильно розмиваючи дрібні деталі та різкі межі об'єктів.
	
	Третій метод — бікубічна інтерполяція. Коли швидкість обчислень не є критичною, перевагу віддають саме їй. Вона враховує вже 16 сусідніх пікселів — вікно 4 на 4. Метод дає значно чіткішу та якіснішу картинку, але через використання кубічних поліномів він має схильність до ефекту "дзвеніння" — появи світлих або темних ореолів навколо дуже контрастних об'єктів.
	
	Власне, розроблений нами барицентричний метод на сітках Чебишева покликаний запропонувати якісну альтернативу цим алгоритмам, перевершуючи їх у збереженні структурної чіткост
	
\section{Вузли Чебишева}
Введемо поняття точок дискретизації. Хоча в математичній літературі зустрічаються такі терміни, як точки Чебишева-Лобатто, екстремуми Чебишева або точки Чебишева другого роду, найчастіше використовується узагальнена назва — вузли Чебишева. Нехай задано натуральне число $n$. Розглянемо сітку з $n+1$ рівномірно розподілених точок на дузі верхньої половини одиничного кола. З геометричної точки зору, їхнє розташування задається набором кутів (аргументів), що змінюються з фіксованим кроком на відрізку від $0$ до $\pi$.

\begin{center}
	fig
\end{center}

Проєкція цих точок на дійсну вісь дає класичне визначення точок екстремумів поліномів Чебишева другого роду

\section{Порівняння рівномірної сітки та сітки по вузлах Чебишева}
Відомо що наближення фінкцій на рівномірній сітці вузлів дає погані результати...

\section{Оптимізований}
Пряме обчислення двовимірної барицентричної формули є обчислювально надмірним. Воно вимагає виконання подвійного вкладеного підсумовування для кожного цільового пікселя, що дає катастрофічну асимптотичну складність O(N4).

Щоб вирішити цю проблему, ми застосували математичну факторизацію. На слайді наведено теорему про розділення змінних: двовимірне ядро розкладається на два послідовні одновимірні проходи. Функція $R_i(y)$ виконує незалежну інтерполяцію по стовпцях, результат якої зберігається у проміжній матриці. Потім зовнішня функція згортає цей проміжний результат по рядках. Завдяки цьому складність алгоритму падає до $\mathcal{O}(N^2)$.

На базі цієї математики ми реалізували два інженерні рішення для максимальної швидкодії:
\textbf{По-перше}, кешування ваг. Оскільки барицентричні ваги залежать виключно від геометрії сітки і не залежать від кольору пікселів, ми розраховуємо їх лише один раз.
\textbf{По-друге}, безблокова паралелізація обчислень. Двопрохідна схема ідеально розпаралелюється. Кожен потік обробляє свій горизонтальний сегмент матриці і пише дані у власну виділену пам'ять. Це дозволило нам повністю відмовитись від \texttt{mutex}-блокувань.

\textbf{Щодо результатів оптимізації:} математичний аналіз доводить, що коефіцієнт прискорення лінійно залежить від розміру зображення. Наприклад, при збільшенні стандартного кадру 500 на 500 пікселів у два рази, кількість ітерацій процесора зменшується більш ніж у \textbf{330 разів}. Це перетворило важку математичну теорію на ефективне рішення, здатне працювати в реальному часі».

\section{Матричний}
Наступним логічним етапом оптимізації розробленого методу став перехід від алгебраїчного запису до його строгої матричної форми.

Цей крок зумовлений важливою математичною властивістю барицентричної інтерполяції: наші вагові коефіцієнти залежать виключно від просторової геометрії сітки (координат вихідних і цільових точок) і жодним чином не пов'язані з колірною інтенсивністю пікселів. Це дозволяє повністю відокремити обчислення геометричних ваг від обробки колірних каналів.
На слайді представлено фінальну аналітичну модель декомпозиції. Шляхом повної нормалізації одновимірних ваг $\alpha$ та $\beta$ нам вдалося звести складну задачу двовимірного просторового масштабування до компактного базового рівняння лінійної алгебри: $\mathbf{G} = \mathbf{B} \mathbf{F} \mathbf{A}^{\top}$.

Де матриця F — це вихідна колірна матриця зображення, а A та B — заздалегідь прораховані просторові матриці переходів по ширині та висоті відповідно.

З інженерної точки зору обчислення цього рівняння виконується у два послідовні проходи:

На першому проході ми перемножуємо матрицю вертикальних ваг B на матрицю зображення F, формуючи проміжний масив даних T.

На другому проході цей масив множиться на транспоновану матрицю горизонтальних ваг $\mathbf{A}^{\top}$, формуючи фінальний растр $\mathbf{G}$.

Перенесення алгоритму на мову матриць дало колосальну практичну перевагу. Воно дозволило відмовитися від складних вкладених циклів на користь лінійно-алгебраїчних операцій високого рівня (GEMM). Таке інженерне рішення забезпечує послідовний, дружній до кешу процесора доступ до пам'яті та відкриває прямий шлях до використання апаратного SIMD-прискорення та векторних інструкцій сучасних обчислювальних систем, що ми й реалізували на базі бібліотеки OpenCV

\section{Матричний OpenCV}
Перехід до матричної форми дозволив нам зробити наступний надважливий крок в оптимізації — делегувати обчислення високорівневій промисловій бібліотеці комп'юторного зору OpenCV.

Оскільки операція просторового масштабування є лінійною, ми розширили нашу математичну модель на кольорові зображення шляхом її поканального застосування. Як ви бачите на формулі у верхній частині слайда, матриця колірних значень вихідного кадру розщеплюється на три незалежні одноканальні матриці за допомогою функції \texttt{cv::split}: для червоного, зеленого та синього каналів окремо. До кожного з них застосовується оптимізоване множення на просторові матриці ваг A та B, після чого канали зливаються назад через \texttt{cv::merge}.

Впровадження цієї схеми в OpenCV (через вбудовану функцію \texttt{cv::gemm}) дозволило задіяти три фундаментальні чинники апаратного прискорення, які ви бачите на слайді:

Векторизація обчислень на базі SIMD-архітектури. Бібліотека на низькому рівні автоматично активує процесорні інструкції SSE, AVX або AVX2. Це дозволяє здійснювати арифметичні операції над цілими векторами даних одночасно, виконуючи обчислення над кількома елементами масиву всього за один такт процесора.

Глибока кеш-оптимізація пам'яті. Замість послідовного перебору елементів, який призводить до затримок через повільну оперативну пам'ять, OpenCV реалізує блокове матричне множення. Велика матриця зображення розбивається на локальні субматриці такого розміру, який повністю вміщується у надшвидку кеш-пам'ять процесора рівнів L1 та L2. Це зводить кількість звернень до RAM до мінімуму.

Прихована багатопотоковість. Розпаралелювання самого процесу множення великих матриць відбувається автоматично на рівні ядра бібліотеки за допомогою сучасних інструментів автоматизації обчислень — таких як Intel TBB або OpenMP. Це дозволяє оптимально завантажити всі доступні обчислювальні ядра сучасного багатоядерного процесора без ручного керування потоками.

Завдяки такій програмній адаптації обчислювальна складність нашого методу перестала бути перешкодою, а сам алгоритм перетворився на високоефективне інженерне рішення, здатне виконувати точне барицентричне масштабування зображень високої роздільної здатності у реальному часі.
\section{Локальна}
Наша глобальна барицентрична формула чудово працює на гладких фотографіях. Проте, якщо ми масштабуємо текст або скріншоти з різкими межами, поліноми високих степенів неминуче породжують "хвилі" поблизу контурів. Це класичний феномен Гіббса.

Для повного придушення цих артефактів ми розробили локальну модифікацію — метод ковзного вікна. Замість того, щоб будувати один величезний поліном для всього зображення, ми працюємо локально.

Алгоритм діє так: для кожного цільового пікселя ми знаходимо його координати у вихідному зображенні і беремо навколо нього дуже маленьке вікно — розміром K на K пікселів. Потім ми відображаємо координати цього вікна на стандартний відрізок від -1 до 1, будуємо локальну сітку Чебишева і застосовуємо нашу формулу лише до цих кількох пікселів.

Це дало два чудові результати:
По-перше, оскільки розмір вікна K — це константа (зазвичай 3 або 5), обчислювальна складність стає ідеально лінійною.
По-друге, ми отримали повний контроль над артефактами. Ми експериментально довели, що вікно розміром 3×3 пікселі (що відповідає квадратичному поліному) зберігає краї об'єктів ідеально різкими і зводить осциляції Гіббса до абсолютного нуля. Збільшення вікна до 5 пікселів дає кращу якість на гладких ділянках, але вимагає компромісу з різкістю контурів
\section{Методологія тестування та оцінки якості}

Для забезпечення високої об'єктивності аналізу та здійснення коректного порівняння якості масштабування розробленого методу з класичними аналогами, було прийнято загальноприйняту в науковій спільноті методологію кількісної оцінки зображень. Оскільки в багатьох стандартних датасетах (наприклад, BSDS500) відсутні вхідні зображення для довільних коефіцієнтів масштабування, було застосовано наступний підхід:

\begin{enumerate}
	\item \textbf{Вибір еталонних зображень:} Усі зображення, що входять до складу тестових датасетів, розглядаються як «цільові» (Target Images), які є еталоном якості та виступають об'єктом порівняння для результатів роботи всіх методів.
	\item \textbf{Генерація вхідних даних (Input Generation):} Для створення уніфікованих вхідних зображень ($I^{in}$) ми застосовуємо метод бікубічної інтерполяції (BIC) до цільового еталонного зображення у протилежному напрямку масштабування.
	\begin{itemize}
		\item Для тестування задачі \textit{upscaling} [$\times s$], вхідне зображення генерується шляхом застосування бікубічної інтерполяції у напрямку [$: s$] (downscaling).
		\item Для тестування задачі \textit{downscaling} [$: s$], вхідне зображення генерується шляхом застосування бікубічної інтерполяції у напрямку [$\times s$] (upscaling).
	\end{itemize}
	Цей підхід дозволяє нівелювати вплив суб'єктивних факторів та забезпечити коректність порівняння результатів роботи різних алгоритмів у рівних початкових умовах, що є стандартом при кількісній оцінці методів обробки зображень[cite: 145, 146, 383, 384].
	
	\item \textbf{Порівняльний аналіз:} Отримані результати масштабування порівнюються з еталонними зображеннями за допомогою кількісних метрик якості:
	\begin{itemize}
		\item \textbf{PSNR (Peak Signal-to-Noise Ratio):} визначає пікове відношення сигналу до шуму, що характеризує енергію помилки реконструкції[cite: 224].
		\item \textbf{SSIM (Structural Similarity Index):} індекс структурної подібності, що оцінює збереження візуальних характеристик, структурних деталей та локального контрасту[cite: 228].
		\item \textbf{MSE (Mean Squared Error):} середньоквадратична похибка, яка виступає статистичною мірою відхилення піксельних значень еталону від результату інтерполяції[cite: 216].
	\end{itemize}
\end{enumerate}

Така методологія дозволяє здійснити об'єктивну верифікацію математичної моделі та підтвердити конкурентоспроможність запропонованого алгоритму на широкому спектрі тестових даних[cite: 344].
	\textbf{[СЛАЙД 3] Математична модель}
	
	З теоретичної точки зору, будь-яке зображення можна представити як двовимірну неперервну функцію просторових координат, задану на квадраті від мінус одиниці до одиниці. Відповідно, цифрове зображення — це просто дискретний набір значень цієї функції, обчислених у певних точках, тобто на сітці вузлів.
	
	У цій роботі для формування такої сітки ми на відміну від статті що була взята за основу використовуємо не нулі Чебишева а екстремуми поліномів Чебишева другого роду. Наша задача масштабування зводиться до пошуку апроксимованих значень функції на новій сітці: більш щільній, якщо ми збільшуємо зображення, або розрідженій, якщо зменшуємо.
	
	Головною математичною особливістю нашого підходу є те, що для максимально ефективної реалізації інтерполяції ми узагальнили відому одновимірну другу барицентричну формулу на двовимірний випадок. Таке узагальнення стало практично можливим завдяки тому, що для побудови барицентричної формули є аналітичні формули для розрахунку коефіцієнтів.
	Варто відзначити, що сама по собі барицентрична формула є універсальною і може бути використана для будь-яких інших систем вузлів. Однак, у такому разі побудова інтерполяційного полінома та розрахунки коефіцієнтів були б значно важчими та ресурсоємнішими. Використання ж саме запропонованих вузлів дозволяє зробити ці обчислення значно простіше.
	\vspace{0.5cm}
	\textbf{[СЛАЙД 4] Програмна реалізація}
	
	Для практичного підтвердження ефективності математичної моделі було розроблено спеціалізоване програмне забезпечення. Програму реалізовано мовою C++ з використанням графічного фреймворку Qt. Використання Qt дозволило створити зручний інтерфейс користувача для завантаження зображень, вибору коефіцієнтів масштабування та візуального порівняння результатів.
	
	Щоб об'єктивно оцінити наш алгоритм, ми порівняли його з еталонними методами з популярної бібліотеки OpenCV, такими як метод найближчого сусіда, білінійна та бікубічна інтерполяція, а також метод Ланцоша. Оскільки глобальна двовимірна інтерполяція є обчислювально важкою задачею, ми оптимізували програму за допомогою засобів багатопотоковості мови C++. Це дозволило розподілити обчислення між ядрами процесора і суттєво зменшити час обробки пікселів.
	
	Якість масштабування оцінювалася за допомогою стандартних метрик: PSNR (пікове відношення сигналу до шуму) та SSIM (індекс структурної подібності). Експерименти показали, що при збільшенні зображень наш метод демонструє якість, порівнянну з класичною бікубічною інтерполяцією. Водночас, при зменшенні зображень він забезпечує значно кращі показники PSNR та SSIM, мінімізуючи втрату деталей.
	
	\vspace{0.5cm}
	\textbf{[СЛАЙД 5-6] Висновки}
	
	Підсумовуючи, у нашій роботі було досліджено та практично реалізовано сучасний математичний підхід до проблеми зміни розміру зображення, що базується на глобальній інтерполяції. Сама теоретична модель була запропонована в статті що була взята за основу, проте нашою головною метою та досягненням стала оптимізація та адаптація нового обчислювально важкого алгоритму для практичного використання. Відповідно до цієї моделі, ми виконуємо апроксимацію зображення до бажаного розміру за допомогою процесів глобальної інтерполяції на основі сіток Чебишева. Зокрема, представлений метод бере значення пікселів будь-якого вхідного зображення для побудови відповідного двовимірного інтерполяційного полінома на основі другої барицентричної формули, обчислення якого дає на виході масштабоване зображення.
	
	Однією з головних переваг цього методу є його висока гнучкість під час роботи в обох напрямках масштабування. Запропонований математичний підхід залишає відкритим шлях для майбутніх розробок та можливих покращень.
	
	Подальшим розвитком програмного забезпечення буде оптимізація методу масштабування зображень з використанням другої барицентричної формули на основі вузлів інтерполяції Чебишева для отримання найкращих результатів по часу розрахунків, автоматизація розрахунку порівняльних характеристик, що призначені для чисельної оцінки якості масштабування та покращення інтерфейсу програмного забезпечення для більш комфортного та інтуїтиво зрозумілого користування.
	
\end{document}